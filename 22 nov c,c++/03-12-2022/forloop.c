/*iteration   itearting varaible
for(initialization;condition;increment/decrement)
{
    body
}
*/
#include <stdio.h>
int main()
{
    int num;
    printf("Enter Number");
    scanf("%d", &num);
    for (int i = 1; i <= 1 0; i++)
    {
        printf("%d * %d=%d\n", num, i, num * i);
    }

    return 0;
}
