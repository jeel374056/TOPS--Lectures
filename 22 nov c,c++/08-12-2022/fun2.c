#include <stdio.h>
void addition(int m, int n)
{
    int answer = m + n;
    printf("Addition=%d", answer);
}
int main()
{
    int m, n;
    printf("Enter Two numbers: ");
    scanf("%d %d", &m, &n);
    addition(x, y);
    return 0;
}