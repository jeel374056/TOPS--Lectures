#include <stdio.h>
#include <string.h>
int main()
{
    char str1[100] = "NGujarat Is a Best place";
    // strrev(str1);
    // strlwr(str1);
    strupr(str1);
    printf("%s", str1);
    return 0;
}