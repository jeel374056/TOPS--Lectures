#include <stdio.h>
#include <string.h>
int main()
{
    char name[200] = "TOPS";
    // printf("%s", strupr(name));
    // printf("%s", strlwr(name));
    // printf("%s", strrev(name));
    return 0;
}