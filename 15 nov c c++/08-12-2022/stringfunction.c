#include <stdio.h>
#include <string.h>
int main()
{
    // int m = 20;
    // int n = m;
    int n1[25] = "hello";
    char n2[25] = "hello";





    
    // int m1 = strcmp(n1, n2);
    printf("%d", sizeof(n1));
    // int length = strlen(n1);
    // printf("%d", length);
    // strcpy(n2, n1);
    // strrev(n1);
    // strcat(n2, n1);
    // strlwr(n1);
    // strlwr(n2);
    // strupr(n1);
    // strupr(n2);
    // printf("n1=%s  N2=%s", n1, n2);
    return 0;
}
/*
String Functions:
strlen()--integer
strcpy(string1,string2)--change in existing one
strrev(string)--change in existing one
strcat(string1,string2)--change in existing one
strlwr()--change in existing one
strupr()---change in existing one
strcmp()--integer
string1=hello  l-108
string2=hello   p-98
strcmp(string1,string2)
-1
+1
0
*/