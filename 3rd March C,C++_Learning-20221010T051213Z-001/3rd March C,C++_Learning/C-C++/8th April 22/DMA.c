// 1. malloc
// 2. calloc
// 3. realloc
// 4. free

#include<stdio.h>
#include<stdlib.h>

int main()
{
    
int *p;
// p = (int*) malloc(20*sizeof(int));
// p = (int*) calloc(20,sizeof(int));
// p[0],p[1];
// printf("2nd value is %d",p[0]);
// printf("2nd value is %d",p[1]);
// p = realloc(p+1,sizeof(int));
// free(p);
return 0;
}