#include <stdio.h>
#include <string.h>

int main()
{
    char s1[100] = "hello";
    int l = strlen(s1);
    printf("Copied string is %s", s1);
    // int l1=strlen(s1);
    // int l2=strlen(s2);
    // int i;
    // for(i=0;i<=l1+l2;i++){
    //     s1[l1+i] = s2[i];
    // }
    // printf("%s",s1);
    return 0;
}